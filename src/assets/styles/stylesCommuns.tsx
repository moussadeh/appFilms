import { StyleSheet } from "react-native";

const stylesCommuns = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }
})

export default stylesCommuns;